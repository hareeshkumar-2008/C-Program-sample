multiple and divide two numbers in the form of (a*b=ans a/b=ans)

#include <stdio.h>

void main()
{
    int a,b,product,diff;
    
    printf("Enter two numbers");
    scanf("%d%d",&a,&b);
    
    product=a*b;
    diff=a/b;
    
    printf("%d*%d=%d %d/%d=%d",a,b,product,a,b,diff);
    
}

output
      enter the number 25 5
      product=125
      diff=5
____________________________________________________________________________________________________________________________________________________

square and cube of a given number

#include <stdio.h>

void main()
{
    int a,sqr,cube;
    
    printf("Given number");
    scanf("%d",&a);
    
    sqr=a*a;
    cube=a*a*a;
    
    printf("%d*%d=%d %d*%d*%d=%d",a,a,sqr,a,a,a,cube);
    
}

_________________________________________________________________________________________________________________________________________________________________________________

square root of the given number

  # include <stdio.h>
# include<math.h>
int main()
{

    int num,sqr,sqrt1;

    printf("Enter the number to perform the square root :");
    scanf("%d",&num);
    sqrt1=sqrt(num);
    printf("The Square root of the given number %d is %d",num,sqrt1);
    return 8;

}

____________________________________________________________________________________________________________________________________________________________________________________

area and perimeter of square 

# include <stdio.h>
# include<math.h>
void main()
{
  int sqr,area,peri;
  
  printf("value of sqr");
  scanf("%d",&sqr);
 
  area=length*breadth;
  peri=2*(length+breadth);
  
  printf("%d",&area,&peri);

}

_____________________________________________________________________________________________________________________________________________________________________________________

program for checking the given age is eligible to vote or not

#include <stdio.h>

  int main()
{
    int age;
    
    printf("Enter the age");
    scanf("%d",&age);
    
    if(age>=18)
    
        printf("Eligible to vote");
        
    else

        printf("Not eligible to vote");
     
        return 0;
     
}

______________________________________________________________________________________________________________________________________________________________________________

To find the Given no is divisible by 5 or not

 #include <stdio.h>

  int main()
{
   
   int a;
   
   printf("Enter the number");
   scanf("%d",&a);
      
    if(a % 5 == 0)
    
      printf("Given no is divisible by 5");
      
    else
      
      printf("Given no is not divisible by 5");
      
        return 0;
     
}

___________________________________________________________________________________________________________________________________________________________________

































